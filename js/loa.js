(function() {
   // to make global functions more apparent
   var global = this;

   function possibleMoves(x, y, board) { // returns a list of possible moves [(x,y)] by a given checker on a given board
       return [];
   }

   global.LOA_Oracle = function() {
   };
})();
